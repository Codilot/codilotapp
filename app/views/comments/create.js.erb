var reviewSent = $("<div/>", {
  html: "Thank you for your review!",
  "class": "col-sm-12",
})

$("#new_comment").fadeOut(400, function() {
  $(this).html(reviewSent).fadeIn(400);
});

$("#average-rating").attr('data-score', "<%= @product.average_rating %>");

$(".product-reviews").prepend("<%= j render @comment %>");


refreshRating();
