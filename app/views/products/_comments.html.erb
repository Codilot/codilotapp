<hr>
<div class="product-reviews row">
    <% @comments.each do |comment| %>
          <%= render comment %>
    <% end %>
</div>
<div class="text-center">
  <%= will_paginate @comments, previous_label: "Previous", next_label: "Next", inner_window: 1, outter_window: 0 %>
</div>
